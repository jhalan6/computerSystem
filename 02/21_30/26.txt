A.当t中的字符串长度大于s中的字符串长度的时候。
B.针对unsigned类型的减法，不存在负数，用一个大数去减一个小数，
会导致其结果成为一个很大的正数。
C.可以在使用strlen()的时候增加一个到long型的强制类型转换，
这样就可以拿到一个有正负的数,不过这个做法可能会导致强转回int时的截断。
另一种方式是增加一个int型的强制转换，
在字符串长度不是特别大的情况下是可行的。
--->
书上的果然才是最好的。。直接使用
    return strlen(s)>strlen(t);
